/**
 * Interactivity API store (Phase 1 scaffold).
 *
 * Full open/close behavior is implemented in Phase 4.
 */

import { store } from '@wordpress/interactivity';

store( 'structured-mega-menu', {
	state: {},
	actions: {
		toggle() {
			// Implemented in Phase 4.
		},
	},
} );
